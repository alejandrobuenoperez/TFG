{
  "dependencies": {
    "@openzeppelin/contracts": "^4.8.2",
    "bootstrap": "^5.2.3",
    "ethers": "^6.3.0",
    "lite-server": "^2.6.1"
  },
  "scripts": {
    "dev": "lite-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
